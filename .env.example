# Cadena de conexion a Postgres/Supabase.
# La consigues en: Supabase > Project Settings > Database > Connection string.
# Para Vercel/serverless conviene usar la opcion de "Connection pooling"
# (modo transaction, puerto 6543). El formato exacto puede variar segun
# la version de Supabase: revisa el valor real que muestre tu proyecto.
DATABASE_URL=postgresql://postgres:Chilaquiles2026@db.wmsngdsclajaddvpwdjp.supabase.co:5432/postgres
